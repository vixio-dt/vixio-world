---
description: Pre-action checklist - MCP FIRST, then verify before implementing
alwaysApply: true
---

# Pre-Action Checklist

**Before taking ANY implementation action, STOP and run this checklist.**

## When This Applies

This checklist is MANDATORY before:
- Implementing features or components
- Modifying existing code
- Creating new files
- Executing implementation plans
- Making architectural changes
- Writing database queries or schema changes

## The Checklist

```
┌─────────────────────────────────────────────────────────────┐
│  PRE-ACTION CHECKLIST - MCP FIRST, then verify              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ★ 1. MCP-FIRST (DO THIS NOW - NOT OPTIONAL)                │
│       → CALL Context7 for any libraries you'll use          │
│       → CALL Sequential Thinking if 2+ approaches exist     │
│       → CALL Supabase if touching database                  │
│       → CALL next-devtools if writing Next.js code          │
│       → CALL tailwindcss if styling                         │
│       → SHOW what you learned before proceeding             │
│                                                             │
│  □ 2. CONFIDENCE: Do I have 95% confidence I understand?    │
│       → If NO: Ask clarifying questions first               │
│                                                             │
│  □ 3. BRAINSTORM: Is this creative/new work?                │
│       → If YES: Use brainstorming skill first               │
│                                                             │
│  □ 4. PLAN EXISTS: Is there an implementation plan?         │
│       → If NO for complex work: Create plan first           │
│       → If YES: Follow the plan steps                       │
│                                                             │
│  □ 5. SKILL MATCH: Does a skill apply to this work?         │
│       → Check skills table in AGENTS.md                     │
│       → Read and follow the matching skill                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## MCP Quick Reference

| MCP | When to Call | Tool to Use |
|-----|--------------|-------------|
| **Context7** | Any library (Mantine, React, Motion) | `resolve-library-id` → `query-docs` |
| **Sequential Thinking** | Complex decisions, debugging | `sequentialthinking` |
| **Supabase** | Database queries, schema | Query tools |
| **next-devtools** | Routes, server actions, errors | `nextjs_index` → `nextjs_call` |
| **tailwindcss** | Styling, utilities | `search_tailwind_docs` |
| **eslint** | Lint errors | `lint-files` |
| **Playwright** | UI testing | Browser automation |
| **Sentry** | Production errors | `search_issues` |
| **GitHub** | PRs, issues, commits | PR/issue tools |
| **AntVis Chart** | Data visualization | Chart generation |
| **Memory** | Persist decisions | `create_entities` |

## After Implementation

```
┌─────────────────────────────────────────────────────────────┐
│  POST-ACTION CHECKLIST - Before claiming done               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  □ 1. VERIFY: Use verification-before-completion skill      │
│       → Run actual commands to confirm success              │
│       → Evidence before assertions                          │
│                                                             │
│  □ 2. DOCS: Update relevant documentation                   │
│       → docs/current-sprint.md                              │
│       → Implementation plans                                │
│       → Roadmap if scope changed                            │
│                                                             │
│  □ 3. LINT: Check for introduced errors                     │
│       → Run ReadLints on edited files                       │
│       → Fix any new errors                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Quick Reference

| Trigger | Required Action |
|---------|-----------------|
| "implement X" | **MCP first**, then full checklist |
| "create X" | **MCP first**, then full checklist |
| "modify X" | **MCP first**, then full checklist |
| "fix bug" | **MCP first** (Context7 + Sequential Thinking), then systematic debugging |
| "add feature" | **MCP first**, then brainstorm + plan |
| "continue" | **MCP first** (query relevant MCPs), show summary, proceed |
| "/continue" | Same as "continue" - MCP consultation is mandatory |

## Exceptions

Skip MCP + checklist ONLY for:
- Trivial edits (typos, comments, formatting)
- Reading/exploring code (no changes)
- Answering questions (no implementation)
- User explicitly says "skip MCP"

**Default: ALWAYS query MCPs first. Show what you learned. Then implement.**
